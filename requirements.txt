# Build-time dependencies for the content pipeline (scripts/build-content.py).
#
# The site itself ships zero dependencies — these are only needed to RENDER
# content to HTML. Authors never run them: Decap commits, CI builds.
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# Pinned exactly: a silent markdown-rendering change would rewrite every
# published article on the next unrelated build.
PyYAML==6.0.2
mistune==3.1.3
Pillow==11.3.0
